--[[
    FLOW FRUIT FINDER - INTERFACE V22.7
    - Integração Total: lista.im + Img_finder + Core
    - Suporte a Temas: Opções 1-4 ou ID Customizada
    - Criador: Hatsune_inkyt
]]

local THEME_COLOR = Color3.fromRGB(0, 210, 255)
local FALLBACK_ID = "rbxassetid://135908570815238"

-- 1. Notificações Dinâmicas
local function Notify(title, text)
    pcall(function()
        game:GetService("StarterGui"):SetCore("SendNotification", {
            Title = title,
            Text = text,
            Icon = getgenv().FlowImageData or FALLBACK_ID,
            Duration = 3
        })
    end)
end

-----------------------------------------------------------
-- CONSTRUÇÃO DA ESTRUTURA VISUAL
-----------------------------------------------------------
local sg = Instance.new("ScreenGui", game:GetService("CoreGui"))
sg.Name = "FlowFinder_V22_7"

-- Botão de Minimizar
local ToggleBtn = Instance.new("ImageButton", sg)
ToggleBtn.Size = UDim2.new(0, 55, 0, 55)
ToggleBtn.Position = UDim2.new(0, 15, 0, 15)
ToggleBtn.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
ToggleBtn.Image = FALLBACK_ID
Instance.new("UICorner", ToggleBtn).CornerRadius = UDim.new(1, 0)
local StrokeBtn = Instance.new("UIStroke", ToggleBtn)
StrokeBtn.Color = THEME_COLOR
StrokeBtn.Thickness = 2

-- Janela Principal
local MainFrame = Instance.new("Frame", sg)
MainFrame.Size = UDim2.new(0, 320, 0, 190)
MainFrame.Position = UDim2.new(0.5, -160, 0.4, -95)
MainFrame.BackgroundColor3 = Color3.fromRGB(10, 10, 10)
MainFrame.ClipsDescendants = true
Instance.new("UICorner", MainFrame)
Instance.new("UIStroke", MainFrame).Color = THEME_COLOR

-- Imagem de Fundo (Monitorada pelo Img_finder)
local BgImg = Instance.new("ImageLabel", MainFrame)
BgImg.Name = "DynamicBackground"
BgImg.Size = UDim2.new(1, 0, 1, 0)
BgImg.BackgroundTransparency = 1
BgImg.ImageTransparency = 0.85
BgImg.ZIndex = 1
BgImg.Image = "" -- Aguardando o Loader

-- Labels de Texto
local Title = Instance.new("TextLabel", MainFrame)
Title.Size = UDim2.new(1, 0, 0, 35)
Title.Text = "FLOW FRUIT FINDER"
Title.TextColor3 = Color3.new(1, 1, 1)
Title.Font = Enum.Font.GothamBold
Title.TextSize = 18
Title.BackgroundTransparency = 1
Title.ZIndex = 3

local StatusLabel = Instance.new("TextLabel", MainFrame)
StatusLabel.Size = UDim2.new(1, -30, 0, 70)
StatusLabel.Position = UDim2.new(0, 15, 0.5, 0)
StatusLabel.BackgroundTransparency = 1
StatusLabel.Text = "INICIANDO SISTEMAS..."
StatusLabel.TextColor3 = Color3.fromRGB(200, 200, 200)
StatusLabel.Font = Enum.Font.GothamMedium
StatusLabel.TextSize = 14
StatusLabel.TextWrapped = true
StatusLabel.ZIndex = 3

-----------------------------------------------------------
-- ORQUESTRAÇÃO DE MÓDULOS
-----------------------------------------------------------

task.spawn(function()
    -- Carrega o Localizador de Imagens (que por sua vez carrega a lista.im)
    StatusLabel.Text = "CARREGANDO TEMAS..."
    pcall(function()
        loadstring(game:HttpGet("https://raw.githubusercontent.com/inkzinho-fofin/Inkzin_hub/refs/heads/main/Img_finder"))()
    end)
    
    -- Carrega o Core de Funções
    StatusLabel.Text = "CARREGANDO MÓDULO CORE..."
    pcall(function()
        loadstring(game:HttpGet("https://raw.githubusercontent.com/inkzinho-fofin/Inkzin_hub/refs/heads/main/Flow_fruit_core"))()
    end)
end)

-- Loop para atualizar a imagem de fundo assim que o getgenv mudar
task.spawn(function()
    while task.wait(0.5) do
        if getgenv().FlowImageData and BgImg.Image ~= getgenv().FlowImageData then
            BgImg.Image = getgenv().FlowImageData
            ToggleBtn.Image = getgenv().FlowImageData -- Atualiza o botão também
        end
    end
end)

-- Loop de Operação Principal
task.spawn(function()
    while not getgenv().FlowCore do task.wait(1) end
    local Core = getgenv().FlowCore
    
    Notify("SISTEMA PRONTO", "Aproveite o Flow Finder!")

    while true do
        StatusLabel.Text = "VARRENDO SERVIDOR..."
        local fruits = Core.Scan()
        
        if fruits and #fruits > 0 then
            for _, f in ipairs(fruits) do
                Notify("FRUTA DETECTADA", "Indo buscar: " .. f.Name:upper())
                Core.SmartMove(f:GetPivot(), function(msg)
                    StatusLabel.Text = msg:upper()
                end)
                Core.Store(f)
                task.wait(1)
            end
        else
            StatusLabel.Text = "NADA ENCONTRADO.\nTROCANDO DE SERVIDOR EM 5S..."
            task.wait(5)
            Core.Hop()
        end
        task.wait(1)
    end
end)

ToggleBtn.MouseButton1Click:Connect(function()
    MainFrame.Visible = not MainFrame.Visible
end)
