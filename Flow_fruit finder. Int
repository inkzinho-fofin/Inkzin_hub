--[[
    FLOW FRUIT FINDER - INTERFACE V22.4
    - Dependência Total: Img_finder + Flow_fruit_core
    - Criador: Hatsune_inkyt
]]

local THEME_COLOR = Color3.fromRGB(0, 210, 255)
local FALLBACK_IMG = "73770110559942"

-- Notificação Nativa
local function Notify(title, text, icon)
    pcall(function()
        game:GetService("StarterGui"):SetCore("SendNotification", {
            Title = title,
            Text = text,
            Icon = icon or FALLBACK_IMG,
            Duration = 3
        })
    end)
end

-----------------------------------------------------------
-- CARREGAMENTO DE MÓDULOS EXTERNOS
-----------------------------------------------------------
local function LoadModules()
    -- Carrega o localizador de imagens (Img_finder)
    pcall(function()
        loadstring(game:HttpGet("https://raw.githubusercontent.com/inkzinho-fofin/Inkzin_hub/refs/heads/main/Img_finder"))()
    end)
    
    -- Carrega o motor principal (Core)
    pcall(function()
        loadstring(game:HttpGet("https://raw.githubusercontent.com/inkzinho-fofin/Inkzin_hub/refs/heads/main/Flow_fruit_core"))()
    end)
end

LoadModules()

-- Puxa a imagem processada pelo Img_finder
local FinalBgID = getgenv().FlowImageData or FALLBACK_IMG

-----------------------------------------------------------
-- CONSTRUÇÃO DA UI
-----------------------------------------------------------
local sg = Instance.new("ScreenGui", game:GetService("CoreGui"))
sg.Name = "FlowFinder_V22"

-- Botão Toggle (Fixo no ID padrão para estabilidade visual)
local ToggleBtn = Instance.new("ImageButton", sg)
ToggleBtn.Size = UDim2.new(0, 55, 0, 55)
ToggleBtn.Position = UDim2.new(0, 15, 0, 15)
ToggleBtn.Image = FALLBACK_IMG
ToggleBtn.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
Instance.new("UICorner", ToggleBtn).CornerRadius = UDim.new(1, 0)
Instance.new("UIStroke", ToggleBtn).Color = THEME_COLOR

-- Frame Principal
local MainFrame = Instance.new("Frame", sg)
MainFrame.Size = UDim2.new(0, 320, 0, 190)
MainFrame.Position = UDim2.new(0.5, -160, 0.4, -95)
MainFrame.BackgroundColor3 = Color3.fromRGB(10, 10, 10)
MainFrame.ClipsDescendants = true
Instance.new("UICorner", MainFrame)
Instance.new("UIStroke", MainFrame).Color = THEME_COLOR

-- Imagem de Fundo (ID fornecida pelo Img_finder)
local BgImg = Instance.new("ImageLabel", MainFrame)
BgImg.Size = UDim2.new(1, 0, 1, 0)
BgImg.BackgroundTransparency = 1
BgImg.ImageTransparency = 0.85
BgImg.ZIndex = 1
BgImg.Image = FinalBgID

-- Cabeçalhos
local Title = Instance.new("TextLabel", MainFrame)
Title.Size = UDim2.new(1, 0, 0, 30)
Title.Text = "FLOW FRUIT FINDER"
Title.TextColor3 = Color3.new(1, 1, 1)
Title.Font = Enum.Font.GothamBold
Title.TextSize = 16
Title.BackgroundTransparency = 1
Title.ZIndex = 3

local Creator = Instance.new("TextLabel", MainFrame)
Creator.Size = UDim2.new(1, 0, 0, 20)
Creator.Position = UDim2.new(0, 0, 0, 22)
Creator.Text = "by Hatsune_inkyt"
Creator.TextColor3 = THEME_COLOR
Creator.Font = Enum.Font.GothamMedium
Creator.TextSize = 11
Creator.BackgroundTransparency = 1
Creator.ZIndex = 3

local StatusLabel = Instance.new("TextLabel", MainFrame)
StatusLabel.Size = UDim2.new(1, -20, 0, 80)
StatusLabel.Position = UDim2.new(0, 10, 0.5, 5)
StatusLabel.BackgroundTransparency = 1
StatusLabel.Text = "SINCRONIZANDO..."
StatusLabel.TextColor3 = Color3.new(1, 1, 1)
StatusLabel.Font = Enum.Font.GothamBold
StatusLabel.TextSize = 13
StatusLabel.TextWrapped = true
StatusLabel.ZIndex = 3

-----------------------------------------------------------
-- LOGICA DE EXECUÇÃO
-----------------------------------------------------------

ToggleBtn.MouseButton1Click:Connect(function()
    MainFrame.Visible = not MainFrame.Visible
end)

task.spawn(function()
    -- Aguarda a presença do FlowCore na memória global
    local Core = getgenv().FlowCore
    while not Core do
        task.wait(0.5)
        Core = getgenv().FlowCore
    end

    Notify("SISTEMA", "Módulos e Imagem carregados!", FinalBgID)

    while true do
        StatusLabel.Text = "BUSCANDO ITENS..."
        local fruits = Core.Scan()
        
        if #fruits > 0 then
            for _, f in ipairs(fruits) do
                Notify("ITEM ENCONTRADO", "Indo para: " .. f.Name:upper(), FinalBgID)
                Core.SmartMove(f:GetPivot(), function(msg) 
                    StatusLabel.Text = msg:upper() 
                end)
                Core.Store(f)
                task.wait(2)
            end
        else
            StatusLabel.Text = "NADA NO MAPA. TROCANDO SERVIDOR..."
            task.wait(5)
            Core.Hop()
        end
        task.wait(1)
    end
end)
